# Google Cloud Configuration

# Option 1: Use Google Cloud CLI (recommended for development)
# Run: gcloud auth application-default login
# Then only set the project ID below

# Option 2: Use Service Account (for production/automation)
# Uncomment and set the path to your service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# Project ID (required)
GOOGLE_CLOUD_PROJECT=sinnia-gnp

# BigQuery Dataset (optional)
BIGQUERY_DATASET=social_dashboard_table

# Data export settings (optional)
DATA_EXPORT_PATH=./data/